import { TODO_FILTERS, FILTERS_BUTTONS } from "../consts"
import { FilterValue } from "../types"

//esto es una movida de TypeScript...
interface Props {
    filterSelected: FilterValue
}

export const Filters: React.FC<Props> = ({filterSelected, onFilterChange}) =>{
    return(

    )
}